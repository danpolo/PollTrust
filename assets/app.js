const state={election:null,current:null,historical:null};
const fmt=v=>v==null?'אין מספיק נתונים':new Intl.NumberFormat('he-IL',{maximumFractionDigits:2}).format(v);
const fmtDate=v=>new Intl.DateTimeFormat('he-IL',{dateStyle:'medium'}).format(new Date(`${v}T12:00:00`));
function daysUntil(v){const t=new Date(`${v}T00:00:00`),n=new Date(),d=new Date(n.getFullYear(),n.getMonth(),n.getDate());return Math.max(0,Math.ceil((t-d)/86400000))}
function expected(m,d){return m?.expected_error_by_days?.[String(Math.max(0,Math.min(120,d)))]??null}
function support(m){if(!m)return'אין היסטוריה';const i=m.independent_election_count??m.election_count??0,s=m.shared_prior_effective_elections??0;return s>0?`${i} עצמאיות + ${fmt(s)} משקל היסטורי משותף`:`${i} מערכות בחירות`}

const HELP={
expected_error:'ממוצע שגיאת המנדטים ההיסטורית באותו מרחק מיום הבחירות. בכל מערכת נבחר רק הסקר האחרון שהיה זמין עד נקודת הזמן, וסקר ישן ביותר מ־45 ימים אינו נכלל.',
overall_raw_error:'ממוצע מרחק העברת המנדטים של הסקר האחרון הזמין לפני כל מערכת בחירות. כל מערכת בחירות מקבלת משקל שווה.',
truth_bias:'הממוצע של הפער בין תחזית הסוקר לתוצאה בפועל עבור אוסף רשימות היסטורי שמוגדר בנפרד לכל מערכת בחירות. הסימן מציין את כיוון ההטיה.',
debiased_error:'שגיאת המנדטים לאחר תיקון הטיה שנלמד רק ממערכות הבחירות האחרות, ורק עבור מזהי מפלגות שניתנים להשוואה.',
lean_value_added:'כמה הסטייה של הסוקר ממרכז סקרים מאוזן בין אשכולות קירבה אותו לתוצאה האמיתית. ערך חיובי מציין שיפור ביחס למרכז.',
support:'מספר מערכות הבחירות שתומכות במדד. לשתי ישויות Direct Polls הנוכחיות מוצג בנפרד גם משקל היסטורי משותף מופחת; רווח 95% מחושב על היסטוריה ישירה.',
bloc_totals:'סכום המנדטים בכל אחד משלושת גושי התצוגה שהוגדרו במפורש בתצורת הבחירות. הסכומים מחושבים ישירות מווקטור 120 המנדטים ואינם משפיעים על ציוני האמינות.'
};
function infoButton(key,label){return '<button class="info-button" type="button" data-help="'+key+'" aria-label="הסבר על '+label+'" aria-controls="metricHelpPopover" aria-expanded="false">i</button>'}
function metricLabel(label,key){return label+' '+infoButton(key,label)}
function outletLabel(meta){return (meta?.outlet_he||'').trim()||'ללא ערוץ קבוע'}
function blocDefinitions(){return state.election?.presentation_blocs||{}}
function partyBlocMap(){return state.election?.party_presentation_blocs||{}}
function blocCssClass(id){const c=blocDefinitions()[id]?.class_name;return /^[a-z0-9_-]+$/i.test(c||'')?c:'bloc-unconfigured'}
function pollBlocState(p){const defs=blocDefinitions(),map=partyBlocMap(),totals=Object.fromEntries(Object.keys(defs).map(id=>[id,0])),unknown=[];let sum=0;for(const[id,raw]of Object.entries(p?.parties||{})){const seats=Number(raw);if(!Number.isFinite(seats)){unknown.push(id);continue}sum+=seats;const bloc=map[id];if(!bloc||!defs[bloc]){unknown.push(id);continue}totals[bloc]+=seats}const errors=[];if(unknown.length)errors.push('אין שיוך גוש עבור: '+unknown.join(', '));if(Math.abs(sum-120)>.001)errors.push('סכום המנדטים הוא '+fmt(sum)+' במקום 120');return{defs,map,totals,error:errors.join(' · ')}}
function blocSummary(bs){if(bs.error)return '<div class="bloc-summary-error" role="alert"><strong>שגיאת תצורת גושים:</strong> '+bs.error+'</div>';const items=Object.entries(bs.defs).map(([id,d])=>'<div class="bloc-total '+blocCssClass(id)+'"><span>'+d.label_he+'</span><strong>'+fmt(bs.totals[id])+'</strong></div>').join('');return '<div class="bloc-summary" role="group" aria-label="סיכום גושים מתוך 120 מנדטים"><div class="bloc-summary-heading"><h4>מצב הגושים</h4>'+infoButton('bloc_totals','מצב הגושים')+'</div><div class="bloc-totals">'+items+'</div></div>'}
function validateBlocConfig(){const defs=blocDefinitions(),map=partyBlocMap(),issues=[];if(Object.keys(defs).length!==3)issues.push('נדרשים בדיוק שלושה גושי תצוגה');for(const id of Object.keys(state.election.party_names||{})){if(!map[id])issues.push('חסר שיוך עבור '+id);else if(!defs[map[id]])issues.push('שיוך לא מוכר עבור '+id)}for(const p of state.current.polls||[])for(const id of Object.keys(p.parties||{}))if(!map[id]||!defs[map[id]])issues.push('הסקר כולל מפלגה ללא שיוך: '+id);return [...new Set(issues)]}
function renderBlocConfigNotice(){const e=document.querySelector('#blocConfigNotice'),issues=validateBlocConfig();e.hidden=!issues.length;e.textContent=issues.length?'שגיאת תצורת גושים: '+issues.join(' · ')+' — לא יבוצע שיוך אוטומטי.':''}

function mandates(p,bs){const names=state.election.party_names||{};return Object.entries(p.parties||{}).filter(([,seats])=>Number(seats)>0).sort((a,b)=>Number(b[1])-Number(a[1])||String(names[a[0]]||a[0]).localeCompare(String(names[b[0]]||b[0]),'he')).map(([id,seats])=>{const bloc=bs.map[id],label=bs.defs[bloc]?.label_he||'ללא סיווג גוש';return '<span class="mandate-chip '+blocCssClass(bloc)+'" aria-label="'+(names[id]||id)+', '+fmt(seats)+' מנדטים, '+label+'"><span>'+(names[id]||id)+'</span><strong>'+fmt(seats)+'</strong><span class="sr-only">'+label+'</span></span>'}).join('')}
function renderHeader(){const days=daysUntil(state.election.election_date),checked=state.current.last_successful_check||state.current.last_successful_update;document.querySelector('#electionBadge').innerHTML=`<strong>${days}</strong><span>ימים עד הבחירות<br>${fmtDate(state.election.election_date)}</span>`;document.querySelector('#updateMeta').textContent=`בדיקת מקורות אחרונה: ${new Intl.DateTimeFormat('he-IL',{dateStyle:'medium',timeStyle:'short'}).format(new Date(checked))}`;if(state.historical.demo_mode){const e=document.querySelector('#demoNotice');e.hidden=false;e.textContent='גרסת הפיתוח משתמשת בנתוני כיול היסטוריים סינתטיים לצורך בדיקת המודל והממשק. אין להסיק מהם מסקנות אמיתיות על אמינות הסוקרים עד להחלפתם במאגר היסטורי מתועד.'}const age=(Date.now()-new Date(checked))/3600000;if(age>state.election.stale_after_hours){const e=document.querySelector('#staleNotice');e.hidden=false;e.textContent=`אזהרה: בדיקת מקורות הסקרים לא הצליחה כבר יותר מ־${state.election.stale_after_hours} שעות.`}}
function latest(){const m=new Map();for(const p of state.current.polls){const old=m.get(p.pollster);if(!old||p.date>old.date)m.set(p.pollster,p)}return m}
function renderCards(){const map=latest(),days=daysUntil(state.election.election_date);document.querySelector('#pollCards').innerHTML=Object.entries(state.election.pollsters).map(([id,meta])=>{const p=map.get(id),m=state.historical.pollsters[id];if(!p)return '<article class="poll-card empty-card"><div class="card-top"><div><h3>'+outletLabel(meta)+'</h3><p class="pollster-name">'+meta.name_he+'</p></div></div><p class="context-note">לא נמצא כרגע סקר נוכחי מאומת במאגר עבור סוקר זה.</p>'+(meta.lineage_note_he?'<p class="lineage">'+meta.lineage_note_he+'</p>':'')+'</article>';const bs=pollBlocState(p);return '<article class="poll-card"><div class="card-top"><div><h3>'+(p.outlet||outletLabel(meta))+'</h3><p class="pollster-name">מבוסס על '+meta.name_he+'</p></div><span class="date-pill">'+fmtDate(p.date)+'</span></div><div class="mandates"><h4>מפת המנדטים</h4><div class="mandate-list">'+mandates(p,bs)+'</div></div>'+blocSummary(bs)+'<div class="metric-grid"><div class="metric"><span>'+metricLabel('שגיאה היסטורית צפויה כיום','expected_error')+'</span><strong>'+(m?.data_status==='demo'?'הדגמה בלבד':fmt(expected(m,days)))+'</strong></div><div class="metric"><span>'+metricLabel('שגיאת סקר אחרון היסטורית','overall_raw_error')+'</span><strong>'+(m?.data_status==='demo'?'הדגמה בלבד':fmt(m?.overall_raw_error))+'</strong></div><div class="metric"><span>'+metricLabel('תמיכת נתונים','support')+'</span><strong>'+support(m)+'</strong></div></div>'+(meta.lineage_note_he?'<p class="lineage">'+meta.lineage_note_he+'</p>':'')+'<a href="'+p.source+'" target="_blank" rel="noopener noreferrer">מקור נתוני הסקר</a></article>'}).join('')}
function renderHistory(days){document.querySelector('#daysValue').textContent=days===0?'יום הבחירות':days+' ימים';document.querySelector('#historyRows').innerHTML=Object.entries(state.election.pollsters).map(([id,meta])=>{const m=state.historical.pollsters[id]||{},demo=m.data_status==='demo',ci=m.uncertainty_95?fmt(m.uncertainty_95[0])+'–'+fmt(m.uncertainty_95[1]):'אין';return '<tr><td class="pollster-cell"><strong>'+meta.name_he+'</strong><span class="pollster-outlet">'+outletLabel(meta)+'</span></td><td>'+(demo?'הדגמה בלבד':fmt(expected(m,days)))+'</td><td>'+(demo?'הדגמה בלבד':fmt(m.overall_raw_error))+'</td><td>'+(demo?'הדגמה בלבד':fmt(m.truth_bias))+'</td><td>'+(demo?'הדגמה בלבד':fmt(m.debiased_error))+'</td><td>'+(demo?'הדגמה בלבד':fmt(m.lean_value_added))+'</td><td>'+support(m)+'<div class="support">רווח 95%: '+ci+'</div></td></tr>'}).join('')}
let activeHelpButton=null;
function closeHelp(){const p=document.querySelector('#metricHelpPopover');if(!p||p.hidden)return;p.hidden=true;p.removeAttribute('style');if(activeHelpButton)activeHelpButton.setAttribute('aria-expanded','false');activeHelpButton=null}
function openHelp(b){const p=document.querySelector('#metricHelpPopover'),help=HELP[b.dataset.help];if(!p||!help)return;if(activeHelpButton===b&&!p.hidden){closeHelp();return}closeHelp();activeHelpButton=b;b.setAttribute('aria-expanded','true');p.textContent=help;p.hidden=false;const r=b.getBoundingClientRect(),w=Math.min(330,window.innerWidth-20);p.style.width=w+'px';let left=Math.max(10,Math.min(r.right-w,window.innerWidth-w-10)),top=r.bottom+8;if(top+p.offsetHeight>window.innerHeight-10)top=Math.max(10,r.top-p.offsetHeight-8);p.style.left=left+'px';p.style.top=top+'px'}
function wireHelp(){document.addEventListener('click',e=>{const b=e.target.closest('.info-button');if(b){e.preventDefault();openHelp(b);return}if(!e.target.closest('#metricHelpPopover'))closeHelp()});document.addEventListener('keydown',e=>{if(e.key==='Escape')closeHelp()});window.addEventListener('resize',closeHelp);window.addEventListener('scroll',closeHelp,true)}
function wire(){document.querySelectorAll('.tab').forEach(b=>b.addEventListener('click',()=>{closeHelp();document.querySelectorAll('.tab').forEach(x=>x.classList.toggle('is-active',x===b));document.querySelectorAll('.panel').forEach(x=>x.classList.toggle('is-active',x.id===b.dataset.tab))}));const s=document.querySelector('#daysSlider');s.addEventListener('input',()=>{closeHelp();renderHistory(Number(s.value))});wireHelp()}
async function init(){try{const[election,current,historical]=await Promise.all(['data/election.json','data/current-polls.json','data/historical-model.json'].map(u=>fetch(u).then(r=>{if(!r.ok)throw Error(u);return r.json()})));Object.assign(state,{election,current,historical});renderHeader();renderBlocConfigNotice();renderCards();renderHistory(30);wire()}catch(e){document.querySelector('main').innerHTML='<div class="notice notice-danger">לא ניתן לטעון את קובצי הנתונים. בדקו את תהליך הפריסה.</div>';console.error(e)}}
init();
